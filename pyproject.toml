[project]
name = "flower-chicken"
description = "A really messy repo for trying out mkdocs and github actions"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    'tomli >= 1.1.0 ; python_version < "3.11"',
]

[project.optional-dependencies]
test = ["pytest", "ruff"]
docs = ["mkdocs", "mkdocs-material", "mkdocstrings[python]", "mkdocs-click"]