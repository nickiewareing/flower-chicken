# these functions are a load of nothing
# just want to test github actions


def return_greeting(name: str, greeting: str = 'Hi'):
    print(f"Hi {name}")


def add_numbers_in_list(nums):
    return sum(nums)